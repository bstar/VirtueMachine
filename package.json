{
  "name": "virtuemachine",
  "private": true,
  "scripts": {
    "dev": "bash ./modern/tools/dev_stack.sh",
    "dev:stop": "bash ./modern/tools/stop_dev_stack.sh",
    "dev:web": "vite --host 0.0.0.0 --port 8080 --strictPort",
    "dev:browser:autoplay": "bash ./modern/tools/dev_browser_autoplay.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.client-app-shell.strict.json --noEmit && tsc -p tsconfig.net.json --noEmit && tsc -p tsconfig.tests.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
